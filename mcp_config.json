{
  "gmail_mcp": {
    "enabled": true,
    "description": "Gmail operations (send, read, search emails)"
  },
  "zoho_mcp": {
    "enabled": true,
    "description": "Zoho Books operations (invoices, contacts, expenses)"
  },
  "github_mcp": {
    "enabled": false,
    "description": "GitHub operations (repos, files, commits, branches)"
  },
  "filesystem_mcp": {
    "enabled": false,
    "description": "File system operations (read, write, list, move, delete)"
  },
  "discord_mcp": {
    "enabled": false,
    "description": "Discord operations (servers, channels, messages)"
  }
}